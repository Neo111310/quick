# Git Konfiguration

```
git config --global user.name "My NAme"
git config --global user.email "my@mail.me"
git config --global core.editor code
```

Dann kann'S schon los gehen.

# Bitwarden SSH

[Link zum SSH Agent](https://bitwarden.com/help/ssh-agent/)
